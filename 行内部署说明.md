# 中国工商银行外部数据产品管理平台原型——行内部署说明

## 一、交付范围

本交付包为可独立运行的前端概念原型，包含建设管理门户、分行建设与报送门户、业务应用门户及预置演示数据。项目不连接真实生产系统，不包含真实客户信息、银行业务数据、账号口令或外部服务密钥。

## 二、环境要求

- Node.js 20.9.0及以上版本
- npm 10及以上版本
- Windows Server或主流Linux服务器
- 建议内存4GB以上

如部署环境不能访问互联网，请由运维人员提前将npm依赖下载至行内制品库，再通过行内镜像地址安装。

## 三、本地启动

在项目根目录执行：

```bash
npm ci
npm run dev
```

浏览器访问 `http://localhost:3000`。

## 四、生产运行

```bash
npm ci
npm run build
npm run start
```

默认监听3000端口。如需指定端口：

```bash
npm run start -- -p 8080
```

生产环境建议由Nginx或行内统一网关反向代理至应用端口，并按照行内要求配置HTTPS、访问控制和日志留存。

## 五、验收步骤

1. 执行 `npm ci`，依赖安装无报错。
2. 执行 `npm run build`，生产构建成功。
3. 执行 `npm run start`，首页可以正常访问。
4. 分别进入建设管理门户、分行建设与报送门户、业务应用门户。
5. 检查能力建设、模块拖动组装、测试发布、产品运行和结果展示等主要功能。
6. 刷新页面，确认已操作的演示状态仍然保留。

## 六、数据与安全说明

- 页面中的企业、指标、名单和运行结果均为演示数据。
- 当前交互状态保存在浏览器 `localStorage` 中，不写入服务器数据库。
- 本原型未接入行内统一认证、权限管理、数据库、数据湖或外部数据接口。
- 如需生产化，应由行内技术团队补充统一认证、接口鉴权、日志审计、数据库持久化、敏感信息保护和部署安全加固。

## 七、目录说明

- `app/`：页面、样式和演示数据
- `public/`：图片及图标资源
- `package.json`、`package-lock.json`：依赖及版本锁定
- `next.config.ts`：Next.js运行配置
- `行内部署说明.md`：部署和验收要求
- `功能说明.md`：门户和主要功能说明
- `数据说明.md`：演示数据边界说明
